// const fs = require('fs')

// fs.writeFileSync('h.txt', '1')

// let data = fs.readFileSync('h.txt')

// console.log(parseInt(data.toString()) + 1);

// fs.writeFileSync('h.txt', (parseInt(data.toString()) + 1).toString())

// fs.appendFileSync('h.txt', 'CH')

new Uint8Array(Math.pow(2, 8) + 100)